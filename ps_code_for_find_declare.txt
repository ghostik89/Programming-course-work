/*!Поиск объявления функции в исходном коде
	\param[in]: SourceCode - исходный код
	\param[in]: func - исходная функция
	\param[in]: Rows - количество строк в исходном коде
	\return: {row} - место объявления функции
*/
int FindDeclareFunc(const char SourceCode[MAX_ROWS][MAX_LENGTH], const char SourceFunc[MAX_LENGTH], int Rows);
=================================================================================================
Изначально считать, что мы не в теле функции
Изначально считать, что объявление функции не найдено
1.Поиск заголовка или объявления функции в коде
для каждой строкчки исходного кода
	если мы не внутри функции и в строке есть имя искомой функции и есть пара круглых скобочек
			запомнить номер строки
2. Вернуть номер строки